<%= form_with(model: hard_drife, local: true) do |form| %>
  <% if hard_drife.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hard_drife.errors.count, "error") %> prohibited this hard_drife from being saved:</h2>

      <ul>
        <% hard_drife.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
